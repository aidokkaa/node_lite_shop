include site_header
div(class='content')
  h1(class='goods-title') #{goods[0]['name']}
  div(class='goods-flex')
   img(src="img/"+goods[0]['image'], alt="")
   div(class='descr-cost')
       p(class='goods-description') #{goods[0]['description']}
       h4(class='goods-cost') #{goods[0]['cost']} тг
       button(
        class='add-to-cart' 
        data-goods_id=goods[0]['id']) В корзину
include nav
include footer